# Portfolio Notes

## Resume bullet

Implemented a Linux process-scheduling simulator in C with configurable CPU count and time slices; built a priority-based ready queue using `fork`, `SIGCONT`/`SIGSTOP`, System V shared memory, and process-shared POSIX semaphores for scheduling, IPC, synchronization, and preemption.

## Interview topics

- Priority scheduling and time slicing
- Preemptive process control
- Shared-memory IPC
- Process-shared semaphores
- Signal handling and `waitpid`
- Scheduler daemon vs. submission shell
